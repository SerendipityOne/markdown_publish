# 平衡车

[B站完整视频](https://www.bilibili.com/video/BV1Gc411v73h)

使用固件库版本为v1.8.5

## OLED

0.96寸OLED屏

原理图：

<img src="./%E5%B9%B3%E8%A1%A1%E8%BD%A6.assets/image-20250907151617567.png" alt="image-20250907151617567" style="zoom: 50%;" />



在STM32CubeMX中设置PB8为I2C1_SCL,PB9为I2C1_SDA

## MPU6050陀螺仪

<img src="./%E5%B9%B3%E8%A1%A1%E8%BD%A6.assets/image-20250907152658892.png" alt="image-20250907152658892" style="zoom:50%;" />

这里使用软件I2C，在CubeMX中设置PB3,PB4为GPIO_Output

在IIC.c中修改引脚定义`IIC_SCL_PIN GPIO_PIN_4`,`IIC_SDA_PIN GPIO_PIN_3`

```c
#include "IIC.h"
#include "stm32f1xx_hal.h"

/* 定义IIC总线连接的GPIO端口, 用户只需要修改下面4行代码即可任意改变SCL和SDA的引脚 */
#define GPIO_PORT_IIC GPIOB                         /* GPIO端口 */
#define RCC_IIC_ENABLE __HAL_RCC_GPIOB_CLK_ENABLE() /* GPIO端口时钟 */
#define IIC_SCL_PIN GPIO_PIN_4                      /* 连接到SCL时钟线的GPIO */
#define IIC_SDA_PIN GPIO_PIN_3                      /* 连接到SDA数据线的GPIO */
```

在main.c中使用MPU6050

```c
/* Private includes ----------------------------------------------------------*/
/* USER CODE BEGIN Includes */
#include "IIC.h"
#include "inv_mpu.h"
#include "inv_mpu_dmp_motion_driver.h"
#include "mpu6050.h"
#include "oled.h"
#include "stdio.h"
/* USER CODE END Includes */

/* USER CODE BEGIN PV */
float pitch, roll, yaw;
uint8_t display_buf[20];
/* USER CODE END PV */

int main(void) {
  /* USER CODE BEGIN 1 */

  /* USER CODE END 1 */

  /* MCU Configuration--------------------------------------------------------*/

  /* Reset of all peripherals, Initializes the Flash interface and the Systick. */
  HAL_Init();

  /* USER CODE BEGIN Init */

  /* USER CODE END Init */

  /* Configure the system clock */
  SystemClock_Config();

  /* USER CODE BEGIN SysInit */

  /* USER CODE END SysInit */

  /* Initialize all configured peripherals */
  MX_GPIO_Init();
  MX_I2C1_Init();
  /* USER CODE BEGIN 2 */
  OLED_Init();
  OLED_Clear();
  MPU_Init();
  mpu_dmp_init();
  OLED_ShowString(0, 0, "Init Success", 16);
  /* USER CODE END 2 */

  /* Infinite loop */
  /* USER CODE BEGIN WHILE */
  while (1) {
    HAL_Delay(10);
    while (mpu_dmp_get_data(&pitch, &roll, &yaw) != 0);

    sprintf((char*)display_buf, "pitch:%.2f    ", pitch);
    OLED_ShowString(0, 2, display_buf, 16);
    sprintf((char*)display_buf, "roll:%.2f    ", roll);
    OLED_ShowString(0, 4, display_buf, 16);
    sprintf((char*)display_buf, "yaw:%.2f    ", yaw);
    OLED_ShowString(0, 6, display_buf, 16);

    /* USER CODE END WHILE */

    /* USER CODE BEGIN 3 */
  }
  /* USER CODE END 3 */
}

```



使用`mpu_dmp_get_data(&pitch, &roll, &yaw)`获取角度值，并输出到OLED屏上	



## HC-SR04超声波测距

[HC-SR04超声波测距模块详解](https://blog.csdn.net/m0_63235356/article/details/134635107)

<img src="./%E5%B9%B3%E8%A1%A1%E8%BD%A6.assets/image-20250907153344751.png" alt="image-20250907153344751" style="zoom:50%;" />

![image-20250907153436101](./%E5%B9%B3%E8%A1%A1%E8%BD%A6.assets/image-20250907153436101.png)

从时序图中可以看到，我们给HC-SR04发送长达10us的TTL脉冲，然后模块就会进行测距，测距的结果通过回响信号传达

我们只需要提供一个10us以上的脉冲触发信号，然后设置输出回响信号上升沿时开始计时，到下降沿停止计时，并读出时间

$ \text{距离} = \text{高电平时间} \times \text{声速} (340 \text{m/s}) / 2 $



1. 根据原理图，PA2接在EC上（即回响信号），PA3接在TR上（即触发信号），我们在CubeMX中设置PA2为`GPIO_EXIT2`外部中断，PA3为`GPIO_Output`
2. 设置PA2的模式为上升沿/下降沿触发，并打开外部中断![image-20250907154311000](./%E5%B9%B3%E8%A1%A1%E8%BD%A6.assets/image-20250907154311000.png)
	![image-20250907164201886](./%E5%B9%B3%E8%A1%A1%E8%BD%A6.assets/image-20250907164201886.png)
3. 设置PA3GPIO输出模式为高电平输出![image-20250907154356935](./%E5%B9%B3%E8%A1%A1%E8%BD%A6.assets/image-20250907154356935.png)
4. 设置TIM3定时器为内部时钟，TIM1、TIM2、TIM4后续另有用处，设置预分频系数为71
	![image-20250907164257699](./%E5%B9%B3%E8%A1%A1%E8%BD%A6.assets/image-20250907164257699.png)
	$ \text{定时器周期} = \frac{\text{内部时钟（HCLK)}}{\text{PSC} + 1} $，HCLK=72MHz，为了方便计算让PSC=71，得到定时器周期为1MHz，则时钟为1us

### 编写代码

sr04.h

```h
#ifndef __SR04_H__
#define __SR04_H__

#include "stm32f1xx_hal.h"
void get_distance(void);

#endif /* __SR04_H__ */

```

sr04.c

```c
#include "sr04.h"

extern TIM_HandleTypeDef htim3;
uint16_t count;  // 定时器时间
float distance;  // CM/S

void delay_us(uint32_t udelay) {
  __IO uint32_t Delay = udelay * 72 / 8;  //(SystemCoreClock / 8U / 1000000U)
  //见stm32f1xx_hal_rcc.c -- static void RCC_Delay(uint32_t mdelay)
  do {
    __NOP();
  } while (Delay--);
}

void get_distance(void) {
  // PA3触发
  HAL_GPIO_WritePin(GPIOA, GPIO_PIN_3, GPIO_PIN_SET);
  delay_us(10);
  HAL_GPIO_WritePin(GPIOA, GPIO_PIN_3, GPIO_PIN_RESET);
}

void HAL_GPIO_EXTI_Callback(uint16_t GPIO_Pin) {
  if (HAL_GPIO_ReadPin(GPIOA, GPIO_PIN_2) == GPIO_PIN_SET) {  // 上升沿
    __HAL_TIM_SetCounter(&htim3, 0);                          // 定时器清零
    HAL_TIM_Base_Start(&htim3);                               // 开启定时器
  } else {                                                    //下降沿
    HAL_TIM_Base_Stop(&htim3);                                // 关闭定时器
    count = __HAL_TIM_GetCounter(&htim3);
    distance = count * 0.017;
  }
}

```

`HAL_GPIO_EXTI_Callback` 原函数为`__weak void HAL_GPIO_EXTI_Callback(uint16_t GPIO_Pin)` 是 STM32 HAL 库中用于处理 GPIO 外部中断的回调函数。它通过弱声明（*__weak*）定义，允许用户在自己的代码中重写该函数以实现自定义逻辑。

当外部中断发生时，HAL 库会调用 `HAL_GPIO_EXTI_IRQHandler(GPIO_Pin)`。 在 `HAL_GPIO_EXTI_IRQHandler` 中，会自动判断并清除中断标志位，然后调用 `HAL_GPIO_EXTI_Callback`。

当检测到PA2是上升沿时，将定时器清零，并开启定时器；当是下降沿时，关闭定时器并获取定时器的值。

$ \text{distance} = \frac{\text{count}}{1000000} \times \frac{340 \text{(m/s)}}{2} \times 100 \text{(cm)} = \frac{\text{count}}{0.017} (\text{cm}) $



`main.c`

```c
/* USER CODE BEGIN Includes */
#include "IIC.h"
#include "inv_mpu.h"
#include "inv_mpu_dmp_motion_driver.h"
#include "mpu6050.h"
#include "oled.h"
#include "stdio.h"
#include "sr04.h"
/* USER CODE END Includes */

/* USER CODE BEGIN PV */
float pitch, roll, yaw;
uint8_t display_buf[20];
extern float distance;
/* USER CODE END PV */

int main(void)
{

  /* USER CODE BEGIN 1 */

  /* USER CODE END 1 */

  /* MCU Configuration--------------------------------------------------------*/

  /* Reset of all peripherals, Initializes the Flash interface and the Systick. */
  HAL_Init();

  /* USER CODE BEGIN Init */

  /* USER CODE END Init */

  /* Configure the system clock */
  SystemClock_Config();

  /* USER CODE BEGIN SysInit */

  /* USER CODE END SysInit */

  /* Initialize all configured peripherals */
  MX_GPIO_Init();
  MX_I2C1_Init();
  MX_TIM3_Init();
  /* USER CODE BEGIN 2 */
  OLED_Init();
  OLED_Clear();
  MPU_Init();
  mpu_dmp_init();
  OLED_ShowString(0, 0, "Init Success", 16);
  /* USER CODE END 2 */

  /* Infinite loop */
  /* USER CODE BEGIN WHILE */
  while (1) {
    HAL_Delay(100);
    mpu_dmp_get_data(&pitch, &roll, &yaw);

    sprintf((char*)display_buf, "pitch:%.2f    ", pitch);
    OLED_ShowString(0, 2, display_buf, 16);

    get_distance();

    sprintf((char*)display_buf, "distance:%.2f    ", distance);
    OLED_ShowString(0, 4, display_buf, 16);

    /* USER CODE END WHILE */

    /* USER CODE BEGIN 3 */
  }
  /* USER CODE END 3 */
}

```



## MOTOR电机

 电机驱动模块:
![image-20250907161600451](./%E5%B9%B3%E8%A1%A1%E8%BD%A6.assets/image-20250907161600451.png)

### 设置PWM

设置PA11、PA8为TIM1定时器
![image-20250907163547563](./%E5%B9%B3%E8%A1%A1%E8%BD%A6.assets/image-20250907163547563.png)
![image-20250907163647843](./%E5%B9%B3%E8%A1%A1%E8%BD%A6.assets/image-20250907163647843.png)

配置预分频系数，即配置PWM的输出频率，$f = \frac{HCLK}{(PSC + 1)(ARR + 1)}$ ，让$f=10kKz$设置$PSC=0,ARR=7199$

PWM占空比$Duty = \frac{Pulse}{ARR + 1}$ 上面设置$ARR+1=7200$，则$pulse$可为7200，这时占空比为100%为全速运行
![image-20250907165801778](./%E5%B9%B3%E8%A1%A1%E8%BD%A6.assets/image-20250907165801778.png)

### 设置GPIO_Output控制方向

![image-20250907170016661](./%E5%B9%B3%E8%A1%A1%E8%BD%A6.assets/image-20250907170016661.png)

motor.h

```h
#ifndef __MOTOR_H__
#define __MOTOR_H__

#include "stm32f1xx_hal.h"
void load(int motor1, int motor2);

#endif /* __MOTOR_H__ */

```

motor.c

```c
#include "motor.h"

extern TIM_HandleTypeDef htim1;

int abs(int a) {
  if (a > 0)
    return a;
  else
    return -a;
}

/**
 * @param motor1: AIN的速度 (-7200~7200)
 * @param motor2: BIN的速度 (-7200~7200)
 * 
 */
void load(int motor1, int motor2) {
  if (motor1 < 0) {
    HAL_GPIO_WritePin(GPIOB, GPIO_PIN_12, GPIO_PIN_RESET);
    HAL_GPIO_WritePin(GPIOB, GPIO_PIN_13, GPIO_PIN_SET);
  } else {
    HAL_GPIO_WritePin(GPIOB, GPIO_PIN_12, GPIO_PIN_SET);
    HAL_GPIO_WritePin(GPIOB, GPIO_PIN_13, GPIO_PIN_RESET);
  }
  // 计算PWM占空比
  __HAL_TIM_SetCompare(&htim1, TIM_CHANNEL_4, abs(motor1));

  if (motor2 < 0) {
    HAL_GPIO_WritePin(GPIOB, GPIO_PIN_15, GPIO_PIN_RESET);
    HAL_GPIO_WritePin(GPIOB, GPIO_PIN_14, GPIO_PIN_SET);
  } else {
    HAL_GPIO_WritePin(GPIOB, GPIO_PIN_15, GPIO_PIN_SET);
    HAL_GPIO_WritePin(GPIOB, GPIO_PIN_14, GPIO_PIN_RESET);
  }
  // 计算PWM占空比
  __HAL_TIM_SetCompare(&htim1, TIM_CHANNEL_1, abs(motor2));
}

```

main.c

```c
/* USER CODE BEGIN Includes */
...
#include "motor.h"
...
/* USER CODE END Includes */

int main(){
  ...
  /* USER CODE BEGIN 2 */
  OLED_Init();
  OLED_Clear();
  MPU_Init();
  mpu_dmp_init();
  OLED_ShowString(0, 0, "Init Success", 16);
  
  HAL_TIM_PWM_Start(&htim1, TIM_CHANNEL_1);
  HAL_TIM_PWM_Start(&htim1, TIM_CHANNEL_4);
  load(1000, 1000);
  /* USER CODE END 2 */
  ...
}
```

## 编码器测速

[编码器计数原理与电机测速原理](https://zhuanlan.zhihu.com/p/350368518)

<img src="./%E5%B9%B3%E8%A1%A1%E8%BD%A6.assets/image-20250908093836383.png" alt="image-20250908093836383" style="zoom: 67%;" />

使用定时器自带的编码器功能，选择`TIM2,TIM4`
![image-20250908093816567](./%E5%B9%B3%E8%A1%A1%E8%BD%A6.assets/image-20250908093816567.png)
![image-20250908093939132](./%E5%B9%B3%E8%A1%A1%E8%BD%A6.assets/image-20250908093939132.png)

此处不需要给TIM2、TIM4进行预分频，在前面给TIM3设置预分频71是因为用来给超声波测距，而内部时钟为72MHz，需要预分频将时钟频率调整为1MHz。此处时钟不是来自内部时钟，而是来自电机上的编码器的信号，编码器的信号不是很快所以不需要分频

![image-20250908094821267](./%E5%B9%B3%E8%A1%A1%E8%BD%A6.assets/image-20250908094821267.png)

一个上升沿就可使定时器的计数器+1，配置定时器的编码触发模式（TIM2、TIM4）

![image-20250908094940509](./%E5%B9%B3%E8%A1%A1%E8%BD%A6.assets/image-20250908094940509.png)

### 代码

encoder.h

```h
#ifndef __ENCODER_H
#define __ENCODER_H

#include "stm32f1xx_hal.h"
int read_speed(TIM_HandleTypeDef* htim);

#endif // !__ENCODER_H

```

encoder.c

```c
#include "encoder.h"

// 读取速度
int read_speed(TIM_HandleTypeDef* htim) {
  int speed = 0;
  speed = (short)__HAL_TIM_GetCounter(htim);  // 获取当前计数值
  /*这里用short类型来存储计数值，因为计数值为0~65535，速度有正有负，所以用short类型-32768~32767来存储*/
  __HAL_TIM_SetCounter(htim, 0);  // 归零计数器
  return speed;
}

```

main.c

```c
/* USER CODE BEGIN Includes */
...
#include "encoder.h"
...
/* USER CODE END Includes */

/* USER CODE BEGIN PV */
int encoder_left, encoder_right;  // 编码器数据
...
/* USER CODE END PV */

/* USER CODE BEGIN PFP */
void read_encoder(void);
/* USER CODE END PFP */

int main(){
  /* USER CODE BEGIN 2 */
  ...
  HAL_TIM_Encoder_Start(&htim2, TIM_CHANNEL_ALL);
  HAL_TIM_Encoder_Start(&htim4, TIM_CHANNEL_ALL);
  /* USER CODE END 2 */
    
  /* Infinite loop */
  /* USER CODE BEGIN WHILE */
  while (1) {
    read_encoder();
    sprintf((char*)display_buf, "encoder_l:%d   ", encoder_left);
    OLED_ShowString(0, 2, display_buf, 16);
    sprintf((char*)display_buf, "encoder_r:%d   ", encoder_right);
    OLED_ShowString(0, 4, display_buf, 16);

    /* USER CODE END WHILE */

    /* USER CODE BEGIN 3 */
  }
}

/* USER CODE BEGIN 4 */
/* 
  用系统滴答定时器延时10ms
  uwTick为系统滴答定时器的计数值
  系统滴答定时器每1ms产生一次中断，计数值递增1 
*/
void read_encoder(void) {
  if (uwTick - sys_tick < 10) return;
  sys_tick = uwTick;  // 更新系统时钟

  encoder_left = read_speed(&htim2);
  encoder_right = -read_speed(&htim4);  // 两个电机转的方向不一样
}

/* USER CODE END 4 */
```

## JDY-31蓝牙串口

<img src="./%E5%B9%B3%E8%A1%A1%E8%BD%A6.assets/image-20250908103722464.png" alt="image-20250908103722464" style="zoom:67%;" />

![image-20250908104014836](./%E5%B9%B3%E8%A1%A1%E8%BD%A6.assets/image-20250908104014836.png)



PB10设置为TX发送引脚，连接蓝牙模块的RXD接受引脚，PB11相反。

配置串口3为异步，设置波特率为9600（与蓝牙模块规定的一致），并打开全局中断（用来接收数据）

![image-20250908104205856](./%E5%B9%B3%E8%A1%A1%E8%BD%A6.assets/image-20250908104205856.png)

![image-20250908104317116](./%E5%B9%B3%E8%A1%A1%E8%BD%A6.assets/image-20250908104317116.png)

### 代码

main.c

```c
/* USER CODE BEGIN PV */
...
extern uint8_t rx_buffer[2];      // 串口接收缓存
/* USER CODE END PV */

int main(){
  /* USER CODE BEGIN 2 */
  ...
  HAL_UART_Receive_IT(&huart3, rx_buffer, 1);                          // 开启串口接收中断，每接收到一个字节就调用一次回调函数
  HAL_UART_Transmit(&huart3, (uint8_t*)"Hello World!\r\n", 13, 1000);  // 发送数据到串口
  /* USER CODE END 2 */
}
```

stm32f1xx_it.c 接收数据

```c
/* USER CODE BEGIN PV */
uint8_t rx_buffer[2];
/* USER CODE END PV */

/**
  * @brief This function handles USART3 global interrupt.
  */
void USART3_IRQHandler(void) {
  /* USER CODE BEGIN USART3_IRQn 0 */

  /* USER CODE END USART3_IRQn 0 */
  HAL_UART_IRQHandler(&huart3);
  /* USER CODE BEGIN USART3_IRQn 1 */
  HAL_UART_Transmit(&huart3, rx_buffer, 1, 1000);  // 把接收到的发回去

  HAL_UART_Receive_IT(&huart3, rx_buffer, 1);  // 继续接收数据
  /* USER CODE END USART3_IRQn 1 */
}
```

## 平衡小车的PID算法

[PID深度解析（基于STM32平衡小车）](https://blog.csdn.net/Carbon6/article/details/106580443)

[PID算法如何让平衡车“站稳脚跟”？](https://blog.breadq.top/firmware/balance)

# PID算法

[PID控制算法原理](https://zhuanlan.zhihu.com/p/39573490)

PID算法应用非常广泛，小到控制一个元件的温度，大到控制无人机的飞行姿态和飞行速度等等，都可以使用PID控制。

PID(proportion integration differentiation)其实就是指**比例，积分，微分控制**。

![image-20250908150320595](./%E5%B9%B3%E8%A1%A1%E8%BD%A6.assets/image-20250908150320595.png)

公式：$U(t) = kp\left(err(t) + \frac{1}{T_I}\int err(t)dt + \frac{T_D derr(t)}{dt}\right)$



总的来说，当得到系统的输出后，将输出经过比例，积分，微分3种运算方式，叠加到输入中，从而控制系统的行为.。

括号内第一项是比例项，第二项是积分项，第三项是微分项，前面仅仅是一个系数。很多情况下，仅仅需要在离散的时候使用，则控制可以化为$u(k) = K_p e(k) + \frac{K_p T}{T_i} \sum_{n=0}^{k} e(n) + \frac{K_p T_d}{T} \big(e(k) - e(k - 1)\big)$

每一项前面都有系数，这些系数都是需要实验中去尝试然后确定的，为了方便起见，将这些系数进行统一一下：$u(k) = K_p e(k) + K_i \sum_{n=0}^{k} e(n) + K_d \big(e(k) - e(k - 1)\big)$

这样看就清晰很多了，且比例，微分，积分每个项前面都有一个系数，且离散化的公式，很适合编程实现。
讲到这里，PID的原理和方法就说完了，剩下的就是实践了。在真正的工程实践中，最难的是如果确定三个项的系数，这就需要大量的实验以及经验来决定了。通过不断的尝试和正确的思考，就能选取合适的系数，实现优良的控制器。

<font color="red">**可以这样理解PID：P是纠正曾经，根据误差逐步靠近目标；I是反省现在，自己是否还在原地踏步；D是代表未来，根据未来的趋势做出控制**</font>





















































